{
  "name": "healthapp-root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r --filter './packages/**' --filter './apps/**' test",
    "typecheck": "pnpm -r --filter './packages/**' --filter './apps/**' typecheck"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.6"
  }
}
